services:
  backend:
    build: ./app/backend
    container_name: pyback
    network_mode: "host"
  nginx:
    image: nginx:latest
    volumes:
      - ./frontend/html:/var/www/html/index.html
      - ./frontend/nginx:/etc/nginx/sites-available/default
